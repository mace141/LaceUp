import React from 'react';

const TeamsIndex = ({ teams }) => {
  return (
    <> 
      {/* {teams.map(team => (
        <TeamIndexItem team={team}/>
      ))} */}
    </>
  )
};

export default TeamsIndex;